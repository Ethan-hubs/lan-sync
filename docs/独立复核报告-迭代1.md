# 迭代 1 独立复核报告（太极 → Codex）

| 项 | 内容 |
|---|---|
| 复核对象 | `main` @ `2e965a4`（7 个提交：`5fab17c` → `2e965a4`），**未推送远端**，经 `git bundle` 取回 |
| 复核时间 | 2026-09-14 00:09 CST |
| 复核环境 | Linux 6.17.0-1013-azure x86_64 · .NET SDK **10.0.401**（与 `global.json` 一致）· 自建一对干净 Syncthing **v2.1.5**（GUI 8394/8395、同步 22011/22012，关官方 discovery/relay/STUN） |
| 复核方式 | **不采信自述**：静态审查源码 + 在**我方机器上实跑其测试套件**（两种夹具模式各跑一遍） |
| 结论 | ✅ **通过** —— 单元 **22/22**、集成 **6/6**（外部实例模式）、集成 **6/6**（自建实例模式） |
| 原始证据 | `/data/lan-sync/review/iter1-test-output.txt`；代码快照 `/data/lan-sync/review/iter1`、`/data/lan-sync/review/iter1-linux` |

---

## 1. 复核怎么做的

1. **取代码**：Codex 未推送远端，且其所在机器（华为笔记本 `D:\GPTWork\lan-sync`）只在一个 SSH 窗口内可达 → 用 `git bundle create --all` 取回（348 KB），在我方 clone。
2. **静态审查**：逐文件读 `src/` 与 `tests/`（共 1646 行 C#），对照 G1 契约与《Codex-交接与任务书 v1.1》§10 的硬约束逐条核对。
3. **实跑**：在我方 Linux 上执行其测试套件。
   - 因为 4 个工程都声明 `net10.0-windows`（Linux 上无法构建），我在**副本**里把 TFM 改为 `net10.0` 并去掉 `TargetPlatform*` 属性；**其余代码一字未改**。
   - 唯一另一处改动：自建实例模式里的 Windows 二进制 SHA-256 常量按 OS 分支（见 §4 P1-2），否则 Linux 上自建模式必然失败。
   - **对外部实例模式（我要求的接口）未做任何改动**，直接 `export LANSW_TEST_A_GUI/APIKEY…` 注入我方实例。
4. **验证清理**：测试结束后核对其 `DisposeAsync` 是否真清理（见 §3 末行）。

---

## 2. 实跑结果（原始输出节选）

```
### 太极独立复核 · 2026-09-14 00:09:51 CST · Linux 6.17.0-1013-azure x86_64 · SDK 10.0.401

### 单元测试
Passed!  - Failed: 0, Passed: 22, Skipped: 0, Total: 22, Duration: 456 ms - LanSync.Syncthing.Tests.dll (net10.0)

### 集成测试（外部实例注入：A=8394 B=8395，真实 Syncthing v2.1.5）
Passed!  - Failed: 0, Passed: 6, Skipped: 0, Total: 6, Duration: 21 s - LanSync.IntegrationTests.dll (net10.0)

### 集成测试（自建实例模式：LANSW_SYNCTHING_BIN 指向 Linux 二进制，测试自起两个临时实例）
Passed!  - Failed: 0, Passed: 6, Skipped: 0, Total: 6, Duration: 23 s - LanSync.IntegrationTests.dll (net10.0)
```

> 单元测试计数核对：12 个 `[TestMethod]`，其中连接映射用 `[DataRow]` 展开 11 例 → 12 − 1 + 11 = **22** ✅ 与自述一致。

---

## 3. 硬约束逐条核对

| 约束（来自交接件 §10） | 核对方式 | 结论 |
|---|---|---|
| 只动 `src/`、`tests/`、根级构建文件 | `git log --name-only 5fab17c~1..HEAD` | ✅ 7 个提交仅触及 `src/`、`tests/`、`LanSync.sln`、`global.json`、`Directory.Build.props`、`.gitignore`；`docs/`、`adapter/`、`m05-toolkit/` **零改动** |
| 不写 UI / 授权 / 账号 / 数据库 | 全树扫描 | ✅ 无 WPF/WinForms/DB/HTTP 服务端代码；仅 `System.Text.Json` + `HttpClient` + MSTest |
| 无 Docker / Python 运行依赖 | 工程文件与测试夹具 | ✅ 无 |
| `net10.0-windows` + 最低 Win10 1809 | `grep TargetFramework/SupportedOSPlatformVersion` | ✅ 4 个工程均为 `net10.0-windows` + `10.0.17763.0`（另见 §4 P1-1 的可用性意见） |
| `versioning.params.maxAge` **字符串 + 单位是秒** | 源码 `CreateFolderConfiguration` + 单测 `Add_folder_serializes_staggered_max_age_as_seconds_string` | ✅ 默认 `2592000`，序列化为字符串；**这条我已实测**（v2.1.5 回读 `"2592000"`） |
| 配置更新 GET→改→PUT，不丢未知字段、无变化不发 PUT | 单测 3 例 + 源码 `ApplyChanges/MergeChanges` | ✅ 未知字段保留、`DeepEquals` 判等后跳过 PUT |
| 重启处理 | `RestartIfRequiredAsync`：判 `restart-required` → POST restart → 轮询 ping | ✅ |
| 版本恢复事务（pause→restore→判空→resume） | 源码检索 | ➖ **未实现**（按迭代 1 范围本就只到 C1–C6，属**下一迭代**；本条规则保留给 C7） |
| `folder/errors` **只读**，不做清理 | `SyncthingAdapter.FolderErrors.cs` + 单测 | ✅ 仅 `GET /rest/folder/errors`；同时兼容"顶层数组"与 `{"errors":[…]}` 两种返回形态 |
| 忽略规则走 `/rest/db/ignores`，不暴露 `.stignore` | `SetIgnoresAsync/GetIgnoresAsync` + 单测 | ✅ 路径与请求体正确，folder 名做了 URL 编码 |
| 连接状态映射（tcp-\*/quic-\*→Direct，relay-\*→Relay，未连→Offline，其余→Unknown，paused 单独表达） | 单测 11 例数据驱动 | ✅ 含 `tcp-future`/`quic-future`/`relay-future`/`unknown-transport` 与 paused 标志 |
| 外部实例注入 + 未提供则自建 + 都不可用则 skip | `SyncthingPair.CreateAsync` + 实跑两种模式 | ✅ 变量只设一半会显式抛错（好）；不可用时 `Assert.Inconclusive`（=skipped，不会把 `dotnet test` 判红） |
| 二进制 SHA-256 校验 | 源码常量与我方既有基准比对 | ✅ `36a0f7bc…f50344` 与官方 v2.1.5 Windows 发行包一致（我方此前独立核过同一值） |
| 测试隔离：串行、随机 Folder ID、专用临时目录 | `[DoNotParallelize]`、`lansw-c1-c6-<guid>`、`%TEMP%/LanSync.IntegrationTests/<guid>` | ✅ 实跑后核查：临时树已清理、实例上自建 folder 配置已删除、共享实验台（8384/8385）**未受影响** |

---

## 4. 发现的问题（分级，均不影响本轮通过）

**P1-1 · 全部工程 TFM 都是 `net10.0-windows`，Linux/CI 无法构建**
`LanSync.Core`、`LanSync.Syncthing` 与两个测试工程都不含 Windows API 依赖（唯一 Windows 分支在测试的 `LongPath` 辅助类里），却继承了 `-windows` TFM。后果：任何 Linux/macOS CI 或第三方复核都必须先改副本（我这次就是）。建议：**库与测试改 `net10.0`，只有将来托盘工程保留 `net10.0-windows` + `SupportedOSPlatformVersion`**。改完我方即可全自动复核（`git clone` + 环境变量 + `dotnet test`），不再需要人工改动。
*注：这也符合"不要无谓抬高 OS 门槛"的同一逻辑——库不需要 Windows 目标框架。*

**P1-2 · 自建实例模式把 Windows 二进制哈希写死**
`SyncthingPair.RequiredWindowsSha256` 是常量，非 Windows 上自建模式必然失败。建议按 OS 选择期望值，或支持 `LANSW_SYNCTHING_SHA256` 覆盖（我已备好 Linux 值 `ab0ea5f3…4ce07f`，官方 v2.1.5 linux-amd64 解包二进制）。

**P1-3 · C5 用全局 `/rest/system/pause`**
全局暂停会连带暂停该实例上其它 folder —— 对"共享实例"场景有副作用。建议契约测试优先用**按设备暂停**（`?device=`），或在测试文档里明确"集成实例必须专用"。真正的"恢复事务暂停范围"规则（只暂停本次需要、原先非 paused 的对端，`finally` 只恢复该集合）属于下一迭代 C7。

**P2-4 · `GetConnectionsAsync` 对非法 device 键会整表失败**
`new DeviceId(key)` 校验 8 段，遇到非标准键会抛异常导致整次调用失败。建议跳过并记录，而不是整表抛。

**P2-5 · 外部实例模式下会残留 device 条目**
folder 配置已正确清理，但 `AddDeviceAsync` 加的设备条目留在实例上（自建模式整实例销毁，无影响；外部模式会累积）。建议清理或至少在报告里注明。

**P2-6 · 缺 `src/` 的使用说明**
仓库根 README 是"设计与工具仓库"那份。建议补一页：SDK 版本（10.0.401）、四个 `LANSW_TEST_*` 变量与 `LANSW_SYNCTHING_BIN`、skip 语义、`dotnet test` 期望结果。

**P2-7 · 未覆盖 C7 恢复事务**（按迭代 1 范围属预期，不算缺陷，列此备忘）

---

## 5. 结论与下一步

- **迭代 1 通过独立复核**：约束全部满足，测试在我方机器上真实跑绿（22+6+6），清理行为正确。
- 请 **push 到远端**（`main` 或新建 `iter1` 分支皆可）——本轮我靠 SSH 取 bundle，推送后复核可完全自动化。
- 建议顺手做 **P1-1 / P1-2**（各约 10 分钟）：之后我就能在 Linux 上直接 `git clone` + `dotnet test` 完成全自动第三方验证，不再需要改副本。
- **迭代 2 建议范围**：C7 版本恢复事务（`pause(对端) → restore → 响应字典判空 → finally resume`，范围规则见交接件 §10 决定 3）+ G1 增补项 B-1（残留 `~syncthing~*.tmp` 的**只读呈现**先做，**清理动作**另立一项、需用户显式确认）。

—— 太极，2026-09-14 00:15 CST

---

## 6. 二次复核（远端 `main` @ `4a812fc`，2026-09-14 00:29 CST）

Codex 修完 P1-1 / P1-2 并推远端后，本轮**首次做到"零改动复核"**：直接 `git clone`（无任何改写），在我方 Linux + SDK 10.0.401 上跑全部四种模式。

| 模式 | 命令要点 | 结果 |
|---|---|---|
| (a) 单元测试 | `dotnet test tests/LanSync.Syncthing.Tests` | ✅ **22/22**（587 ms） |
| (b) 集成 · 外部实例（我方一对 v2.1.5，8394/8395） | `LANSW_TEST_A/B_GUI/APIKEY` 注入 | ✅ **8/8**（6 × C1–C6 + 2 × SHA 选择）21 s |
| (c) 集成 · 自建实例（**Linux 二进制，未设任何哈希覆盖**） | `LANSW_SYNCTHING_BIN=…/syncthing` | ✅ **8/8** 23 s —— 证明按 OS 自动选哈希真的生效 |
| (d) 集成 · 无实例 | 显式清空全部 `LANSW_*` | ✅ **2 passed / 6 skipped**（C1–C6 全 skip，不会把 `dotnet test` 判红） |
| (e) 代码格式 | `dotnet format --verify-no-changes` | ✅ 退出码 0 |

**变基完整性**：Codex 的变基重写了首轮 7 个提交的哈希，但 `git diff <旧 tip> <新 tip> -- src tests` **输出为空**（内容逐字一致，仅哈希变化）。旧 → 新对应：

| 首轮（bundle，未推） | 远端 main | 内容 |
|---|---|---|
| `5fab17c` | `d514475` | build: initialize .NET 10 solution |
| `27fd053` | `cd8de13` | feat: REST client + health |
| `dbefde4` | `0d7e9ea` | feat: safe configuration updates |
| `1f4f9eb` | `dc16ec4` | feat: devices/folders/ignores |
| `3e2e99f` | `c40b92f` | feat: connection mapping |
| `13e9ce2` | `073b231` | feat: events + pause/resume |
| `2e965a4` | `60393e4` | test: C1–C6 contract checks |

**我的文档提交未被覆盖**：`6f5ef3b`、`e5a3ed3`、`d4fb34a` 在远端历史中均为 `HEAD` 的祖先 ✅（变基 + 非强制推送按承诺完成）。

**一条自测提醒（不是缺陷，供后续复核者避坑）**：我第一次跑「无实例模式」得到的是 8/8，因为**我自己的 shell 里遗留了 `LANSW_SYNCTHING_BIN`**（bash 会话变量在工具调用间持久）。用 `env -u LANSW_SYNCTHING_BIN -u LANSW_TEST_*` 显式清空后才是 2 passed / 6 skipped。复核无实例模式时务必显式清空这四个变量。

### 问题状态更新

| 编号 | 状态 |
|---|---|
| P1-1 全工程 `net10.0-windows` | ✅ **已关闭**（四个工程改为 `net10.0`，Linux/macOS CI 可直接构建；零改动复核由此成为可能） |
| P1-2 自建模式哈希写死 | ✅ **已关闭**（按 OS 选择 + `LANSW_SYNCTHING_SHA256` 覆盖，且覆盖值做了格式校验，新增 2 个单测守护） |
| P1-3 C5 用全局 pause | ⏳ 留待后续迭代（Codex 声明 4 条非阻塞项不混入本次修复，符合"一次只做一件事"） |
| P2-4 连接表非法键整表抛 | ⏳ 后续 |
| P2-5 外部模式残留设备条目 | ⏳ 后续 |
| P2-6 缺 `src/` 使用说明 | ⏳ 后续 |
| P2-7 C7 恢复事务未覆盖 | ⏳ 迭代 2 主体 |

### 结论

**迭代 1 及其两项改进均通过独立复核**，且复核通道已从"改副本"升级为"零改动全自动"。迭代 2 建议范围：C7 版本恢复事务（`pause(对端) → restore → 响应字典判空 → finally resume`，范围规则见交接件 §10 决定 3）+ G1 增补 B-1 的只读呈现部分。

—— 太极，2026-09-14 00:35 CST

### 6.1 复现性提醒：实现机（华为笔记本）上 `dotnet test` 目前不可直接复现

我从 SSH 实测（`D:\GPTWork\lan-sync`）：

```
> dotnet --version            →  8.0.301 [C:\Program Files\dotnet\sdk]
> dotnet test                 →  A compatible .NET SDK was not found.
                                 Requested SDK version: 10.0.401
                                 global.json file: D:\GPTWork\lan-sync\global.json
                                 Installed SDKs: (空)
```

也就是说：**该机器 PATH 上的 .NET 只有 8.0.301，而仓库 `global.json` 钉的是 10.0.401** → 谁在这台机器上直接 `dotnet test` 都会失败。但该机确实存在过 10.0.401 的构建产物与 SDK 使用痕迹（`%USERPROFILE%\.dotnet\10.0.401.dotnetFirstUseSentinel`、`bin/Debug/net10.0-windows/*`），说明 Codex 是在某个能拿到 SDK 10 的环境里跑的（未走这台机器的默认 PATH）。

**建议（项目发起人/Codex 二选一）**：
1. 在这台机器上装一次机器级 SDK 10（`winget install Microsoft.DotNet.SDK.10`），之后你本人也能一键复现 `dotnet test`；
2. 或在仓库 README 里写明"构建环境约定"（SDK 版本 + 若为非常规安装，给出实际路径/环境变量），避免"实现方能跑、验收方跑不了"。

> 与我方复核的关系：**不影响结论**——我在自己机器上用 10.0.401 零改动跑通了全部四种模式；此处只是提醒"实现机的默认环境还不具备可复现性"。

### 6.2 一条 P2（架构权衡，非缺陷）

四个工程改成 `net10.0` 后，Windows 平台分析器（CA1416 / `SupportedOSPlatformVersion`）不再作用于库 —— 将来若库要直接调用 Windows API（例如在库层做 DPAPI 保护 API key），编译期就不会再给出"该 API 在 Win10 1809 不可用"这类警告。建议：**保持库平台中立**，把平台相关调用收在托盘工程（保留 `net10.0-windows` + `10.0.17763.0`）；若确实需要在库里调用，再给该库加 `net10.0-windows` 作为第二目标框架。

---

## 7. 发布事故：`de82600` 误删实现方代码（太极责任，已修复并加守卫）

**事故**：太极的 `de82600`（提交说明只写"文档更新"）实际删除了实现方推上来的
`src/`、`tests/`、`LanSync.sln`、`global.json`、`Directory.Build.props` 以及 `.gitignore` 里的构建规则——
**41 个文件 / 1816 行**。Codex 发现后用 `ac6bccb` 从已复核过的 `4a812fc` 精确恢复。

**根因（我这边）**：`scripts/build_public.py` 在重建发布暂存区时**清空整个目录（只留 `.git`）**，
然后只写回自己清单里的文件。这个假设在"公开仓库只有文档与工具包"时成立，
但实现方开始把 C# 代码推进同一个仓库后就不成立了——**脚本把不属于自己的文件当成垃圾清掉了**。

**修复（`4645200`）**：
1. 暂存清理**只清空本脚本托管的三个目录**（`docs/`、`adapter/`、`m05-toolkit/`），其余路径一律不删不改；
2. README 改为**标记块合并**（`<!-- BEGIN:太极生成区 -->` … `<!-- END:太极生成区 -->`），
   实现方写的段落完整保留；README 仍会整体过一遍脱敏替换（用户名/主机名/私有域名/IP）；
3. `LICENSE`、`.gitignore` **只在缺失时创建**，绝不覆盖（实现方的 `bin/ obj/ TestResults/ .vs/` 规则因此保住）；
4. 新增**删除守卫**：发布前检查相对 `HEAD` 的删除项，**凡涉及非托管路径立即中止**（退出码 2），宁可失败也不误删。

**验证（都在远端复跑过，不是看日志）**：

| 检查 | 结果 |
|---|---|
| `git diff 4a812fc ac6bccb -- src tests LanSync.sln global.json Directory.Build.props` | **空** —— 恢复逐字一致 |
| `git clone` 远端后 `src/ tests/` 与根构建文件同 `4a812fc` 比对 | ✅ 完全一致 |
| `.gitignore` 含 `bin/ obj/ TestResults/` | ✅ 实现方规则未被覆盖 |
| README 含双方内容且不重复（`## 文档`×1、`## 构建环境约定（C#）`×1） | ✅ |
| 修复后重跑发布脚本 | ✅ 删除守卫通过、隐私扫描 0 命中、仅 `README.md` 与 `adapter/` 内文件变更 |
| 从恢复后的远端 `git clone` 零改动跑测试 | ✅ 单元 **22/22**、集成 **8/8**（真实 v2.1.5 实例） |

> 后续发布流程固定为：**先 `git -C public-repo pull --rebase` → 再跑 `build_public.py`**（已写进脚本头部说明）。
> 这条事故与修复也说明：**跨执行方共用仓库时，"发布脚本的清理范围"必须显式等于"脚本的所有权范围"**。

## 8. 复核 README 新写的「构建环境约定（C#）」：路径与现状不符

Codex 在 README 记录了 Windows 实现机使用的 SDK 路径：

```
C:\Users\<user-a>\AppData\Local\Microsoft\dotnet10\dotnet.exe
```

**我在那台机器上实测：该路径不存在。**

```
> "C:\Users\<user-a>\AppData\Local\Microsoft\dotnet10\dotnet.exe" --version
系统找不到指定的路径。
> dir "C:\Users\<user-a>\AppData\Local\Microsoft\*"      → 无 dotnet10/ 目录
> dir "C:\Users\Administrator\AppData\Local\Microsoft\dotnet*" /b → 空
> dir "C:\Users\Administrator\.dotnet" /b                → 无 sdk
> dir /s /b D:\GPTWork\dotnet.exe  D:\Tools\dotnet.exe  → 空
> dotnet --version（PATH）→ 8.0.301 [C:\Program Files\dotnet\sdk]
```

即：**这台机器上目前既找不到 .NET 10 SDK，`global.json` 又钉着 10.0.401** —— 记录的那条路径无法复现。
可能原因是该 SDK 曾临时存在（`%USERPROFILE%\.dotnet\10.0.401.dotnetFirstUseSentinel` 是唯一残留痕迹），
或在实现方自己的沙箱环境里，事后被清理。

**建议（二选一，都可 10 分钟完成）**：
1. 在实现机上装一次机器级 SDK 10（`winget install Microsoft.DotNet.SDK.10`），让 README 的"构建环境约定"变成**任何人照抄就能跑**；
2. 若确实使用非常规安装位置，请在 README 里写**当前实际存在**的路径，并附一条自检命令（`<path>\dotnet.exe --version` 应输出 `10.0.401`）。

> 与我方复核的关系：**不影响已有结论**——我这边用 10.0.401 零改动跑通了全部模式；
> 但"实现机跑不了、记录的路径又不存在"会让后来者无法复现，值得在迭代 2 开工前顺手收掉。

—— 太极，2026-09-14 00:55 CST
